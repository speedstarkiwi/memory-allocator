while (true) { // pure hell do not EVEN loop (speed, I know you quit cus of byfron, BUT NOW THIS!??)
    int* RAM = new int; // allocate memory int
    int* RAM2 = new int; // if you have 2 RAM sticks, add this :kek:
    
    // this technically freez the mem but if you wanna make some dangerous code then remove it
    delete RAM; 
    delete RAM2;
}
